# =============================================================================
# Final Expense Voice Bot — Python Dependencies
# =============================================================================
#
# Install: pip install -r requirements.txt
#
# For GPU servers, also install:
#   pip install nemo_toolkit[asr] torch torchaudio soundfile
# =============================================================================

# --- Web framework & async ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
aiohttp>=3.10.0
pyVoIP>=1.6.8

# --- Configuration ---
python-dotenv>=1.0.0
pyyaml>=6.0.0

# --- Data processing ---
numpy>=1.26.0

# --- Type checking / dev ---
pydantic>=2.9.0

# --- Audio ---
soundfile>=0.12.0

# --- Semantic search / RAG embedding ---
sentence-transformers>=2.7.0

# --- NeMo ASR (Parakeet TDT speech-to-text) —REQUIRED for STT —
nemo_toolkit[asr]>=2.1.0

# --- PyTorch (CUDA 12.1) —— REQUIRED for LLM, STT, TTS inference ---
# Note: Installed separately via deploy script with CUDA index for perf
# torch>=2.5.0
# torchaudio>=2.5.0

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
