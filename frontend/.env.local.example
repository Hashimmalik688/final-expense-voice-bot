# frontend/.env.local — local development overrides (not committed)
# Copy this to .env.local and adjust for your environment.

# URL the Next.js API route uses (server-side) to call the bot
API_URL=http://localhost:9000

# URL shown to the browser (not currently used but kept for reference)
NEXT_PUBLIC_API_URL=http://localhost:9000
